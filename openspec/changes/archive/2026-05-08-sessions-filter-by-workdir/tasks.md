## 1. 后端查询能力改造

- [x] 1.1 设计并实现会话查询参数：`platform_id`、`path_filter`、`offset`、`limit`。
- [x] 1.2 在后端查询流程中先同步生成目录列表（去重、排序、包含 `all` 与 `unknown` 语义）。
- [x] 1.3 在同一数据源上按 `path_filter` 过滤后执行分页，返回 `sessions + pagination`。
- [x] 1.4 若缺失统一路径字段，在会话摘要结构补充 `path` 字段并完成序列化。

## 2. 前端接入与交互

- [x] 2.1 在前端 API 层扩展会话查询入参（目录筛选 + 分页参数）。
- [x] 2.2 在 Sessions 工具栏新增路径 `select` 控件，默认 `all`。
- [x] 2.3 路径选项改为使用后端返回的 `paths`，不在前端自行全量计算。
- [x] 2.4 切换路径时重置 `offset = 0` 并重新查询。
- [x] 2.5 切换平台时重置筛选为 `all`，并查询新平台目录列表+第一页结果。

## 3. 列表渲染与稳定性

- [x] 3.1 保持现有分页交互（加载更多/翻页）在路径筛选下行为正确。
- [x] 3.2 过滤后空列表时展示专用空状态文案。
- [x] 3.3 验证与会话恢复、会话删除功能无冲突。

## 4. 长路径展示与本地化

- [x] 4.1 调整路径显示样式，确保 very long path 可完整可见（不截断）。
- [x] 4.2 在 `src/locales/zh-CN.json` 与 `src/locales/en.json` 添加路径筛选文案。

## 5. 测试与验收

- [x] 5.1 后端测试：目录列表生成、`all/unknown/具体路径` 三类筛选、分页正确性。
- [x] 5.2 前端验证：路径切换请求参数正确、平台切换重置 `all`、长路径完整显示。
- [x] 5.3 手工验证至少 Claude/Codex 两类来源路径筛选。
- [x] 5.4 验收：默认显示全部、按路径筛选准确、分页稳定、切换平台重置筛选。
